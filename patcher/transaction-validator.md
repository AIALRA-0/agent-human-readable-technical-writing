# 补丁事务验证

## 1. 写入前检查

- 当前文档摘要与每个补丁绑定的摘要一致
- `node_id` 仍指向同一字符范围
- `old_text` 在文档和授权节点中恰好出现指定次数
- 所有修改区间互不重叠
- 修复范围属于 Finding 授权范围

## 2. 内存验证

提交器先在内存中应用全部替换；局部验证和全文验证都通过以后才进入原子写入

## 3. 回滚行为

任一准备检查、冲突检查或验证器失败时，原文件保持逐字不变；写入使用同目录临时文件和原子替换，避免产生半写入内容
