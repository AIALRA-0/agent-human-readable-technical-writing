# 用户全局中文写作偏好

这些规则适用于所有面向用户的中文回答、计划、状态更新、报告、笔记、表格、注释和交付文档

## 默认读者

- 假设读者第一次接触项目，不知道内部名称、英文缩写、行业术语或历史背景
- 先写读者需要知道的事实、原因、后果和边界，再提供技术名称和原始证据
- 不得让技术名词或状态名称承担解释工作

## 表达顺序

- 判断、风险、限制和建议默认按照“具体原因或证据 → 实际后果 → 结论或行动”书写
- 每个结论必须在同一句或紧邻前文获得支撑；不得只写有风险、不能判断或需要处理
- 直接回答是否完成、是否批准或是否安全时，可以先给结果，但必须立即说明依据和未证明的边界
- 普通陈述采用“主体 → 动作或状态 → 结果”；没有真实对比时，删除“已经完成的是、需要关注的是”等延迟主语结构

## 视觉结构

- 两个以上能够分别理解的事实、原因、对象、步骤或行动不得挤在同一行
- 名词列举、句子级并列和排比分项换行；只有不可拆分的固定组合可以留在句内
- 一个条件只有一个结果时，在同一项目中写成“如果条件，结果”
- 一个条件对应多个行动或后果时，条件单独成项，后续内容换行缩进
- 列表承担解释或决策作用时，列表前说明为什么列出，列表后说明共同结论和下一步
- “如下、这样写、包括以下、分为以下”等引出后文的句子使用冒号；分号只用于结束陈述

## 术语

- 技术词首次出现时立即写中文名称并解释当前作用和后果；不得先使用，最后才补定义
- 英文缩写写成“缩写 中文全称（英文全称）”；普通英文名词写成“中文全称（英文原名）”
- 英文全称采用官方写法；没有官方写法时，主要单词首字母大写；数学符号、统计符号、计量单位和程序标识保留规范大小写
- 数学公式排版语法（LaTeX）使用标准结构显示公式和符号；公式、上下标、希腊字母、不等式和统计符号优先使用这种格式；行内公式使用 `$...$`，独立公式使用 `$$...$$`
- 代码、命令、网址、界面原文和逐字证据不得为了数学排版而改写
- 正文括号外不得保留未翻译的小写英文；逐字证据、代码、网址和必要内部标识除外
- 能用十五个字以内的普通中文准确替代时，直接替换术语
- 正文只保留理解结论所需的解释；名称由来和背景细节放入悬停注释、脚注或术语索引
- 决定结论的原因和后果不得藏在悬停注释或术语表中
- 不输出“作用解释：”或带有多个字段标签的括号定义；把必要解释改成自然短句

## 流程图

- 流程关系优先使用文本流程图（Mermaid）
- 默认从上到下排列，让阅读顺序和执行顺序一致
- 横向比较更重要或竖向排列严重增加页面长度时，才改用横向排列
- 图中节点使用普通中文说明动作或状态，不得让内部名称承担解释工作
- 图前说明使用目的，图后说明共同结论或下一步

## 标点

- 禁止使用中文句号字符 U+3002
- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号，直接结束即可
- 冒号、问号、感叹号和省略号仍可按语义保留
- 普通解释、通俗改写、比喻和强调不得使用装饰性引号
- 逐字引用、界面文字、原始状态名称和确实需要定义的名称可以使用引号

## 标题

- 一个标题只表达一个主题
- 禁止使用斜杠、含糊字母或“和、与、及”连接多个独立分类

## 代码注释

- 所有面向用户展示的代码都必须带有帮助第一次阅读者理解的注释
- 命令、配置项、键值项和彼此独立的语句属于列表式代码；每个有独立作用的代码行都要使用该语言允许的同行注释，简要说明这一行做什么
- 函数、类、条件、循环、查询和界面组件属于段落式代码；每个逻辑段开头都要使用该语言允许的独立注释，说明这一段为什么存在和会产生什么结果
- 注释必须使用代码语言本身允许的写法，不得为了满足形式要求破坏语法、复制结果或可执行性
- 严格数据格式、逐字证据或必须原样复制的代码如果无法合法加入注释，不得直接展示成无注释代码块；改为正文解释或文件链接，并另给带注释的等价示例

## 交付检查

- 逐段确认第一次阅读者能回答“这是什么、为什么重要、会造成什么”
- 检查每个判断是否有依据，每个行动是否有目的，每个列表是否换行，每个分支结构是否与结果数量匹配，每个引出句是否使用冒号
- 检查所有代码块是否已经采用列表式同行注释或段落式开头注释
- 检查流程关系是否优先使用从上到下排列的文本流程图（Mermaid）
- 检查是否仍然存在“作用解释：”或字段标签式括号定义
- 检查中文句号、行尾中文分号、装饰性引号、写作套话、延迟主语、连续多个“的”、未解释英文和未使用 LaTeX 的数学记号
- 明确区分流程验证、真实硬件验证和产品发布
- 生成或修改中文技术内容时，使用 `human-readable-technical-writing` 个人写作规范技能；这个技能控制因果顺序、术语解释和视觉结构
