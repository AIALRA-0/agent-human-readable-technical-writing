# 用户全局中文写作偏好

这些规则适用于所有面向用户的中文回答、计划、状态更新、报告、笔记、表格、注释和交付文档

## 1 默认读者

- 假设读者第一次接触项目，不知道内部名称、英文缩写、行业术语或历史背景
- 先写读者需要的事实，再说明后果和边界，最后提供技术名称和原始证据
- 不得让技术名词或状态名称承担解释工作

## 2 表达顺序

- 判断、风险、限制和建议按照“具体原因或证据 → 实际后果 → 结论或行动”书写
- 每个结论必须在同一句或紧邻前文获得支撑
- 直接回答是否完成、是否批准或是否安全时，可以先给结果，随后立即说明依据和未证明范围
- 普通陈述采用“主体 → 动作或状态 → 结果”
- 每个动作、状态、判断和责任都要写明对应主体
- 前文存在多个可能对象时，直接重复准确名称，不使用“它、其、该对象、该系统”等模糊指代
- 前后句更换主体时，在发生切换的句子中重新写明主体
- 避免 `不是……而是……`、`并非……而是……` 和 `不在于……而在于……` 等否定先行结构，先写具体原因或事实，再写实际结论
- 避免双重否定，改成直接肯定或单一否定
- 删除“先说结论、简单来说、需要注意的是”等写作套话
- 能用准确动词时，不写 `进行检查`、`进行分析`、`执行验证` 等名词化结构
- `执行命令`、`任务已完成` 等真实操作和结果状态保留原样
- 一个句子主要承载一个动作或一条因果关系，证据、后果和范围形成三次以上转折时拆句
- 改写只重组用户提供的事实，不能补写没有来源的原因、结果、数据或责任判断
- 同一解释单元只完整说明一次证据范围或发布边界

## 3 内容结构

### 3.1 章节

- 内容需要分成多个部分时，使用 `1`、`1.1`、`1.2.3` 这种十进制层级编号
- 编号从 `1` 开始，不使用 `0` 作为第一节
- 文档主标题不编号，后续标题编号需要与层级一致
- 一个标题只表达一个主题
- 禁止使用斜杠、含糊字母或连接词合并多个独立分类
- 标题默认使用陈述式主题
- 常见问题、访谈记录、问答页面或用户明确要求保留问题时，才使用疑问句标题
- 不写“为什么本项目禁止形式化验证”，需要写“本项目禁止形式化验证的原因”
- 不写“历史证据怎样独立保存”，需要写“历史证据的独立保存方式”

### 3.2 步骤

- 操作过程使用“第一步 安装依赖”“第二步 运行检查”这种中文顺序
- 相邻步骤之间保留一个空行
- 步骤内部存在多个独立动作时，再换行列出动作

### 3.3 换行

- 两个以上能够分别理解的事实、原因、对象或行动换行展示
- 一个条件只有一个结果时，在同一项目中写完条件和结果
- 一个条件对应多个行动或后果时，条件单独成项，后续内容换行缩进
- 列表承担解释或决策作用时，列表前说明为什么列出，列表后说明共同结论和下一步
- 同一逻辑框架中的简短标签和值保留在同一行
- 不把简短版本号、器件名、状态值或日期机械拆到下一行

正确写法：

Vivado 芯片设计套件（Vivado Design Suite）负责完成芯片设计处理

项目 Vivado 冻结版本为：2024.1

目标器件冻结为：`xcvu19p-fsva3824-1-e`

## 4 术语

- 原文已经使用的正式术语、英文缩写和内部名称必须保留，保证读者仍能使用原名称核对证据和开展沟通
- 术语和解释共同保留；术语提供可追踪名称，解释提供第一次阅读所需的理解
- 不主动删除正式术语，也不允许术语独自承担解释工作
- 技术词首次出现时立即写中文名称，再用自然短句说明它是什么、为什么重要以及失败会造成什么
- 英文缩写写成“缩写 中文全称（英文全称）”
- 普通英文名词写成“中文全称（英文原名）”
- 英文全称采用官方写法，没有官方写法时主要单词首字母大写
- 数学公式、上下标、希腊字母、不等式和统计符号优先使用数学公式排版语法（LaTeX）
- 行内公式使用 `$...$`，独立公式使用 `$$...$$`
- 代码、命令、网址、界面原文和逐字证据保持原样
- 正文括号外不保留未翻译的小写英文，必要内部标识除外
- 决定结论的原因和后果不得藏在悬停注释或术语表中
- 不输出“作用解释：”或带有多个字段标签的括号定义
- 版本、状态、限制、期限和结果需要写明所属对象
- 不写 `项目冻结版本`，需要写 `项目 Vivado 冻结版本` 或其他实际被冻结对象

## 5 流程图

- 流程关系优先使用文本流程图（Mermaid）
- 默认从上到下排列，让阅读顺序和执行顺序一致
- 横向比较更重要或竖向排列严重增加页面长度时，才改用横向排列
- 图中节点使用普通中文说明动作或状态
- 流程图和图题默认共同居中，不使用空格模拟居中
- 图前说明使用目的，图后写编号图题并说明共同结论

## 6 数值来源

- 每个业务数值、测量结果、比较结果、阈值、配置值、预测值和经验值都要说明来源
- 外部引用数值在附近使用顺序引用编号，并在文末提供完整来源
- 计算结果写明输入数值、公式或换算方法
- 软件或实际应用产生的数值写明系统、界面、日志、配置文件、仪器或记录名称
- 用户提供的数值明确写成“根据用户本次提供的数据”
- 经验值和估计值明确标记，并说明经验样本、适用条件或不确定范围
- 同一来源覆盖一组连续数值时，在这组数值前统一说明即可
- 章节编号、步骤编号、引用编号、版本标识和程序内部标识不要求额外来源
- 来源无法确认的数值标记为“来源待核对”，不能把它当作已经证实的事实

## 7 图表

- 文档存在编号章节时，图表编号使用“所属一级章节号.本章序号”
- 第 1 章的前两个图依次写成“图 1.1”“图 1.2”，第 2 章的第一个表写成“表 2.1”
- 图形和表格在每个一级章节中分别从 `1` 开始计数，进入新章节后重新计数
- 文档没有编号章节时，图形和表格分别使用“图 1”“表 1”这种单一连续编号
- 禁止使用“图 0.1”“表 0.1”或跨章节连续累计的编号
- 表格、图片、流程图及其题注默认在页面中共同居中
- 支持网页标签的 Markdown 轻量标记语言（Markdown）文档使用 `<div align="center">` 包住对象和题注，并在标签与对象之间保留空行
- Word 文字处理文档（Microsoft Word）、PDF 便携式文档格式（Portable Document Format）、幻灯片和网页使用目标格式的原生居中设置
- 纯轻量标记语言阅读器不支持居中时，明确说明版式限制并保留可读内容
- 不使用连续空格、全角空格或空白列模拟页面居中
- 表题统一放在表格上方，编号章节中的写法为“表 1.1 候选结果”
- 图题统一放在图片或流程图下方，编号章节中的写法为“图 1.1 验证流程”
- 补充说明放在图表下方，以“注：”开头
- 改编或转载内容在题注中给出引用编号

## 8 引用

- 引用采用 IEEE 电气电子工程师学会（Institute of Electrical and Electronics Engineers）顺序编码格式
- 正文按照首次引用顺序使用 `[1]`、`[2]`
- 重复引用同一来源时沿用原编号
- 文末参考文献按照编号顺序列出
- 每个正文编号都需要对应文末条目
- 不使用作者年份式引用

## 9 标点

- 禁止使用中文句号字符 U+3002
- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号
- 冒号、问号、感叹号和省略号可以按语义保留在行尾
- 引出列表、引文、示例或后续正文时使用冒号
- 普通解释、通俗改写、比喻和强调不使用装饰性引号

## 10 代码注释

- 所有面向用户展示的代码都必须带有帮助第一次阅读者理解的注释
- 命令、配置项、键值项和彼此独立的语句属于列表式代码
- 列表式代码中，每个能独立产生作用的代码行都要使用该语言允许的同行注释
- 不得只注释重要行，也不得用一条总注释代替多条独立语句
- 函数、类、条件、循环、查询和界面组件属于段落式代码
- 段落式代码的每个真实逻辑段开头都要使用该语言允许的独立注释
- 不能为了减少注释而把独立语句错误归类成连续逻辑段
- 严格数据格式无法合法加入注释时，交付原文件链接，再提供带注释的等价示例

## 11 交付检查

- 逐段确认第一次阅读者能回答“这是什么、为什么重要、会造成什么”
- 检查每个判断是否有依据，每个行动是否有目的
- 检查每句话是否写明实际主体
- 检查多个对象同时出现后的代词是否仍然只有一个明确指向
- 检查前后句更换主体时是否重新写明新主体
- 检查每个决定结论的数值是否写明来源
- 检查每个计算结果是否写明输入和计算方法
- 检查来源无法确认的数值是否明确标记为待核对
- 检查多章节内容是否使用从 `1` 开始的十进制层级编号
- 检查操作步骤是否使用中文顺序，并在步骤之间保留空行
- 检查并列内容是否换行，同一逻辑框架中的短标签和值是否保持同行
- 检查版本是否写明所属对象
- 检查状态是否写明所属对象
- 检查限制是否写明所属对象
- 检查期限是否写明所属对象
- 检查结果是否写明所属对象
- 检查是否存在双重否定
- 检查图表是否具有独立编号
- 检查图表题注的位置是否正确
- 检查图表是否具有必要注释
- 检查引用是否使用顺序编码格式
- 检查所有代码块是否采用列表式同行注释或段落式开头注释
- 检查流程关系是否优先使用从上到下排列的文本流程图
- 检查是否仍然存在字段标签式括号定义
- 检查中文句号和行尾中文分号
- 检查装饰性引号和写作套话
- 检查延迟主语
- 检查模糊指代
- 检查缺失动作主体
- 检查连续多个“的”
- 检查未解释英文
- 检查没有使用数学公式排版语法的数学记号
- 明确区分流程验证、真实硬件验证和产品发布
- 生成或修改中文技术内容时，使用 `human-readable-technical-writing` 个人写作规范技能
