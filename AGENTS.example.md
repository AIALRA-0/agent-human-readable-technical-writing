# 用户全局中文写作偏好

这些规则适用于所有面向用户的中文回答、计划、状态更新、报告、笔记、表格、注释和交付文档

## 1 技能调用

- 所有中文写作、讲解和改写任务使用 `human-readable-technical-writing` 技能
- 每次任务先读取核心技能，再按照技能路由只加载当前任务需要的参考文件
- 普通回答不读取技能开发测试，也不运行仓库质量检查

## 2 每次回答的硬规则

- 假设读者第一次接触项目，不知道内部名称、英文缩写、行业术语或历史背景
- 判断、风险、限制和建议按照“具体原因或证据 → 实际后果 → 结论或行动”书写
- 每个动作、状态、判断和责任都要写明主体
- 多个对象同时出现后，重复准确名称，不使用指向含糊的代词
- 正文不得使用 `不是……而是……`、`并非……而是……` 和 `不在于……而在于……` 等否定先行转折
- 正文不得使用双重否定，改成直接肯定或单一否定
- 正式术语和内部名称必须保留，并在首次出现时补充自然解释
- 技术词不能独自承担解释工作
- 不输出“作用解释：”或由类型、含义、影响等字段标签拼成的括号定义
- 每个决定结论的数值都要写明引用来源、计算来源、系统来源、用户输入来源或经验来源
- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号
- 所有面向用户展示的代码都要按照独立语句或逻辑段补充注释
- 流程关系优先使用从上到下排列的文本流程图（Mermaid）

## 3 结构化内容

- 多章节内容使用从 `1` 开始的十进制层级编号
- 操作步骤使用“第一步”“第二步”这种中文顺序，相邻步骤之间保留一个空行
- 表格和图形分别编号，编号与所属一级章节一致
- 表题放在表格上方，图题放在图形下方
- 表格、图片、流程图及其题注默认共同居中
- 引用采用 IEEE 电气电子工程师学会（Institute of Electrical and Electronics Engineers）顺序编码格式

## 4 交付门禁

- 写完草稿后扫描完整正文，任何规则命中都要先修改
- 否定先行转折的扫描范围包括同一行、跨行和跨段内容
- 大型任务先逐节检查，再对拼接后的全文检查
- 大型任务使用技能附带的 `scripts/Test-HumanReadableChinese.ps1` 检查最终正文
- 检查通过后发生任何文字改动时，重新检查修改后的完整版本
- 全部问题修复并取得 `PASS` 结果后才能交付
