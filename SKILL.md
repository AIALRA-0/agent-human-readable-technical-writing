---
name: human-readable-technical-writing
description: 所有面向用户的中文回答、中文技术写作、改写、翻译、解释、压缩、README、状态、报告、UI、API、图片、表格和代码说明都使用；把任务编译为基础操作与解释增量，完整保留原文信息，分开追踪原文、用户补充、外部背景和推断，再按照读者与媒介生成自然中文；只要求纯代码、纯 JSON、纯数字或非中文机械格式时不触发
---

# AIALRA 可验证中文写作

## 1. 启动

完整读取 `constitution/principles.md`；该文件规定源信息、来源分层、最小修复和用户金标边界

只处理用户当前要求的内容；没有授权时，不增加发布、账户、权限、安装或其他无关架构

## 2. 编译任务契约

读取 `runtime/task-compiler.md` 和 `runtime/clarification-gate.md`；确定两个独立维度：

- 基础操作：`TRANSFORM`、`TRANSLATE`、`COMPRESS`、`EXPLAIN`、`GENERATE` 或 `FORMAT_ONLY`
- 解释增量：`NONE`、`GLOSS`、`EXPLANATORY`、`TEACHING` 或 `RESEARCHED`

默认配置：

- 只要求翻译：`TRANSLATE + GLOSS`
- 要求翻译到能够看懂：`TRANSLATE + EXPLANATORY`
- 只要求同语种忠实改写：`TRANSFORM + NONE`
- 只要求调整版式：`FORMAT_ONLY + NONE`

同时登记读者、文体、媒介、内容组件和用户配置；任务契约还要登记独立或连续阅读范围、已解释术语、原始组件顺序、排比组、代码覆盖方式、代码同行注释对齐、多轮替换关系、实际渲染证据、证据边界和横向布局例外；结构以 `contracts/task-contract.schema.json` 为准

只有不同解释会改变事实、范围、结论、来源要求或输出规模时才提问；一次说明问题、选项、推荐默认和各选项的实际影响

## 3. 理解原文与来源

改写和翻译读取 `constitution/source-vs-background.md` 与 `runtime/source-understanding.md`

来源固定分为：

- `SOURCE`：原文直接表达，并指向原文位置
- `USER_SUPPLIED`：用户在当前任务中补充
- `EXTERNAL_BACKGROUND`：帮助理解的外部知识，并登记来源
- `INFERENCE`：根据已登记内容推导，并保留置信度

`TRANSFORM` 和 `TRANSLATE` 的源信息覆盖率为 `100%`；原文事实、条件、否定、范围、数值、关系和不确定程度不得丢失或改变

解释增量可以增加必要背景、定义、机制、例子和关系说明；补充内容必须与原文分开追踪，不能伪装成原作者主张，也不能代替原文证据

原意无法确认时停止推断并进入一次性问答确认

## 4. 建立骨架并成文

复杂原文、长文、解释性改写和解释性翻译读取 `runtime/content-blueprint.md` 与 `runtime/constrained-rendering.md`

先把所有源语义单元分配到段落，再插入具有明确用途的背景；没有源信息、背景、推断、组件解释或必要逻辑作用的句子不进入正文

需要确定首屏、决策、状态、审计或项目变化内容时读取 `references/aemp-content-sufficiency.md`；先确定读者必须理解、看到、决定和验证的内容，再分配最多三层展示并执行删除测试

直接根据骨架生成目标风格正文；禁止先全文翻译、再全文解释、再全文润色，因为连续重写会扩大事实漂移

启用 Lucas 配置时读取 `profiles/users/lucas.yaml`；普通中文正文不使用中文句号；分号只用于同一行紧凑并列或专业词说明；多个独立项目必须换行并按层级缩进；段落末尾不保留中文句号或中文分号；逐字引文、日志、代码和用户要求原样保存的材料保持原文

标题按照内容结构按需选择层级；短单主题内容不设置标题；需要分区时使用适当 Markdown 标题，禁止使用 `操作：`、`术语：`、`证据边界：` 等冒号伪标题；自然完整句用于引出后续列表、引文或示例时使用冒号，不把这种语法作用误判为伪标题

独立动作、主体切换和可能产生歧义的位置明确主体；同一明确主体的连续内容允许自然省略，禁止机械重复

两个以上承担相同作用的定义、步骤、比较对象、事实、原因或动作必须换行并缩进；需要使用 `indented_list` 时，每个独立项目必须从对应层级的标准 Markdown 列表标记开始，只有换行、空格缩进或连续散文都不算列表；是否出现冒号不影响判断，项目内部还有并列内容时继续增加一级缩进；压缩任务减少重复表达，同时保留主题边界和必要换行

不同语义作用的排比组必须保持独立结构，例如定义与读数、原因与结果、条件与处理不能压成一个没有组间边界的平面列表；用自然引导句、适当标题或明确的块级间隔区分各组，并让 `parallel_groups` 的登记与实际层级一一对应

追加要求已经替换旧要求时，最终答案只保留当前有效内容；只有用户明确要求变更历史、撤销说明，或者法律、审计与证据核对需要时，才继续展示失效要求

## 5. 按组件加载

- 图片：读取 `profiles/components/images.yaml`；先保留原图，再按覆盖清单说明图片整体展示什么、从哪里开始阅读、每个有效元素的可见特征与位置、功能、元素之间的关系、图片支持的判断和证据限制；只描述位置而不说明元素功能或其对整体结果的作用时，不能形成闭环；素材没有显示功能时明确写无法从图中确认，不凭空补充机制
- 表格：读取 `profiles/components/tables.yaml`；先保留原表的表头、列、行、单元格、顺序和原有数据，不新增“决策提示”等解释列，不把解释写进数据格；需要适配目标媒介时只转换表格外层标记，不改变表格数据，并把表格整体用途以及每一列的名称、单位、允许值或内容范围和实际作用逐项写在表格之外，再补充值词典、行映射、异常格、比较和限制；表格行本身属于 TABLE 结构，不作为散文 `parallel_groups`
- 代码：读取 `profiles/components/code.yaml`；保留原始可执行代码，同行注释属于可修复的解释层，不得改变代码语句、字面值、调用关系和控制条件；选择合法代码注释或逐行独立解释，每个有效语句都要有可定位的说明；使用注释时在每个代码块内计算最长显示宽度 `L`，宽度为 `W` 的行补 `L-W+1` 个空格后写注释标记；两个以上可注释语句必须把代码块正文通过标准输入交给 `runtime/align_inline_comments.py` 机械对齐，再对最终正文运行同一脚本的 `--check`，只有返回 `PASS` 才能输出；允许对齐增加行宽；针对可配置或可迭代输入，所有次数、上限和失败边界必须绑定实际默认值或输入长度，不能把默认值写成函数无条件行为；同时解释工具、动作、原因、语法、输入、结果、失败表现、副作用和查询方法；限制只在影响操作或判断时自然说明
- 来源呈现：短篇逐字文本使用引用块，代码与日志使用代码块，图片与表格保留原始对象；生成结论和普通强调不使用引用块
- 媒介布局：只有任务合同的 `renderer` 明确为 `github_markdown` 时，GitHub 图片和表格才使用居中 HTML，并要求桌面、移动端、亮色和暗色四种实际渲染证据；没有这四种证据时不得声称已经验证居中，也不得伪造证据；其他媒介保留其适用的原始对象格式并记录真实限制
- 术语：读取 `registries/terms.yaml`；独立阅读单元中的确属专业名词首次出现时，按注册表的 `required_meanings` 在同一首次语义位置说明正式中文、官方英文、正式缩写、中文定义、名称含义、当前作用、可观察表现和实际影响；其中 `name_origin` 必须落实为注册表的 `name_rationale` 所表达的名称含义，不能只给出一般定义或把名称解释推迟到术语表；翻译任务默认保留专业名词的官方英文名称；`core_terms`、主题关键词和普通操作短语不自动成为专业名词；没有登记或来源支持的普通任务标签使用自然中文解释，不创造英文形式；只有确属专业名词但官方形式仍无法确认时进入人工复核
- 单位与机器标识：读取 `registries/units.yaml` 和 `registries/protected-patterns.yaml`

## 6. 规则等级

读取 `constitution/hard-vs-advisory.md` 与 `validators/rule-severity.yaml`

- `MACHINE_FINAL`：代码能够确定，失败时阻断
- `MACHINE_CANDIDATE`：代码只定位疑似问题，由上下文判断
- `PROFILE_REQUIRED`：当前任务或用户配置要求，失败时阻断
- `ADVISORY`：用于优化，不单独阻断

普通词语、固定句长、常见语序和单一章节模板不能成为通用硬门

## 7. 自迭代验证与修复

事实、条件、范围、数值、来源和源内容完整性优先于风格；发现确定性问题时读取 `runtime/verification-and-repair.md`

初稿是内部工作结果，不得直接交付；初稿完成后重新打开 `SKILL.md`、当前用户配置、任务命中的组件规则、术语表和验证规则，不依赖第一次阅读时的记忆

每次复核先把用户原话中的必须保留项、正确部分和当前有效要求整理为不可丢失清单，先检查事实、条件、范围、数值、来源、机制和源内容，再检查结构与风格；格式修复不得删除、弱化或用相关位置与表现替代被要求保留的机制和关系

先运行确定性脚本，再执行一次只返回规则、证据位置和最小修复建议的语义复核；合并并去重两类发现以后，只对命中的最小完整单元提交精确补丁

补丁必须绑定文档摘要、单行节点、旧文本、出现次数、修复原因和需要复验的规则；只允许字符、词组或单句范围；低一级范围能够修复时不得扩大；禁止用整段、整节或全文重新生成掩盖局部错误

每轮补丁后重新执行全部适用检查，最多修复三轮；阻断问题清零后只交付最终答案；三轮后仍有问题、补丁摘要失配、定位不唯一或只能大范围重写时返回 `REVIEW_REQUIRED`

证据边界默认只保存在任务合同和验证包；只有缺失证据会影响结论、操作或安全时才在正文中自然说明，禁止把 `证据边界`、`验证边界` 或 `内部边界` 当作用户可见标签

## 8. 用户金标边界

修改本技能、规则、检查器或案例时读取 `constitution/user-gold-standard.md`

模型的内部初稿、自动检查和一至三轮局部修复只属于运行证据；闭环通过的最终快照才能进入 `evals/candidate/`；用户明确接受后才能进入 `evals/gold/`；用户明确拒绝后进入 `evals/rejected/`

脚本通过和模型评分不能替代用户审核；候选案例未审核时，最终状态只能写成“候选版本等待用户审核”
