---
name: human-readable-technical-writing
description: Create, rewrite, review, or validate user-facing Chinese prose for first-time readers, especially technical content. Use for reports, summaries, plans, status updates, notes, tables, and explanations containing technical terms, internal identifiers, causal conclusions, lists, procedures, figures, citations, code, or decision branches
---

# 面向智能体的人类可读技术写作

## 1 执行方法

第一步 确定读者需要判断或执行什么

第二步 找出支撑判断的事实、原因和缺失证据

第三步 按照“原因或证据 → 后果 → 结论或行动”组织内容

第四步 保留原文中的正式术语和内部名称，并在首次出现后立即补充自然解释

第五步 根据内容关系选择段落、列表、步骤、表格或流程图

第六步 为代码补充逐行注释或段落开头注释

第七步 为每个表格和图形补充符合文档用途的编号题注，缺少题注时不能交付

第八步 使用目标格式支持的原生方式，让表格、图片、流程图及其题注共同居中

第九步 按第一次阅读者的理解能力复查，再运行写作检查

## 2 内容结构

### 2.1 章节

- 内容确实需要分成多个部分时，使用 `1`、`1.1`、`1.2.3` 这种十进制层级编号
- 编号从 `1` 开始，不使用 `0` 作为第一节
- 文档主标题不编号，二级标题从 `1` 开始，后续标题编号需要与层级一致
- 一个标题只表达一个主题，不使用斜杠、含糊字母或连接词合并多个独立分类
- 标题默认使用陈述式主题，不使用“为什么、如何、是否、怎样”等疑问句承担章节名称
- 常见问题、访谈记录、问答页面或用户明确要求保留问题时，可以使用疑问句标题
- 把“为什么本项目禁止形式化验证”改成“本项目禁止形式化验证的原因”
- 把“历史证据怎样独立保存”改成“历史证据的独立保存方式”

### 2.2 步骤

- 操作过程使用“第一步 安装依赖”“第二步 运行检查”这种中文顺序
- 相邻步骤之间保留一个空行
- 步骤内部存在多个独立动作时，再换行列出动作
- 概念分类、证据清单和结果矩阵不写成操作步骤

### 2.3 换行

- 两个以上能够分别理解的事实、原因、对象或行动换行展示
- 一个条件只有一个结果时，把条件和结果写在同一项目中
- 一个条件对应多个行动或后果时，条件单独成项，后续内容换行缩进
- 同一逻辑框架中的简短标签和值保留在同一行
- 不把简短版本号、器件名、状态值或日期机械拆到下一行

正确写法：

`项目 Vivado 冻结版本为：2024.1`

目标器件冻结为：`xcvu19p-fsva3824-1-e`

## 3 表达顺序

### 3.1 叙事顺序

- 判断、风险、限制和建议默认按照“具体原因或证据 → 实际后果 → 结论或行动”书写
- 直接回答是否完成、是否批准或是否安全时，可以先给结果，随后立即说明依据和证据范围
- 普通陈述采用“主体 → 动作或状态 → 结果”
- 每个动作、状态、判断和责任都要写明对应主体，不能让读者猜测是谁执行、谁完成或谁承担后果
- 前文存在多个可能对象时，不使用“它、其、该对象、该系统”等模糊指代，直接重复准确名称
- 前后句更换主体时，在发生切换的句子中重新写明主体
- 避免 `不是……而是……`、`并非……而是……` 和 `不在于……而在于……` 等否定先行结构，先写具体原因或事实，再写实际结论
- 避免 `不能不`、`不得不`、`并非不`、`不是没有` 等双重否定，改成直接肯定或单一否定
- 删除“先说结论、简单来说、需要注意的是”等写作套话

### 3.2 动词选择

- 能用一个准确动词表达时，不写 `进行检查`、`进行分析`、`执行验证`、`完成对配置的检查` 等名词化结构
- 把 `进行检查` 改成 `检查`，把 `进行分析` 改成 `分析`，把 `完成了对配置的检查` 改成 `已检查配置`
- `执行命令`、`执行脚本`、`任务已完成` 和 `迁移完成` 表达真实操作或结果状态，可以保留

### 3.3 句子节奏

- 一个句子主要承载一个动作或一条因果关系；证据、后果和范围形成三次以上逻辑转折时拆成短句
- 不使用二十五字统一上限；术语全称、数值来源和必要条件可能需要较长句子，是否拆句取决于逻辑转折数量
- 同一主体连续承担多个动作时，优先使用“主体负责以下工作”加列表；多个主体并存或责任可能混淆时，继续重复准确主体

### 3.4 结论位置

- 不强制全文结论先行；普通分析按照原因到结果书写，完成状态和紧急安全指令可以先给结果或行动
- 长报告的章节开头可以直接概括本节结果，但下一句必须立即提供证据，不能只留下抽象判断

### 3.5 证据范围

- 改写只重组用户提供的事实，不能为了补齐故事而新增故障原因、处理结果、监控数据或责任判断
- 范围说明优先使用正向写法，先写现有证据能够证明什么，再写剩余事项需要哪类证据
- 同一解释单元只完整申明一次发布边界、证据边界或适用范围，后文直接引用已经确定的范围
- 安全、医疗、法律提示或可能被单独摘录的表格和章节需要独立成立时，可以再次保留必要边界

## 4 术语

- 原文已经使用的正式术语、英文缩写和内部名称必须保留，保证读者仍能搜索日志、核对配置和与专业人员沟通
- 术语和解释作为同一个交付单元；术语提供可追踪名称，解释提供第一次阅读所需的理解
- 不主动删除正式术语，也不允许术语独自承担解释工作
- 首次出现时使用“缩写 中文全称（英文全称）”或“中文全称（英文原名）”
- 术语解释放在第一次使用的位置，不先使用数值或结论，最后才解释名称
- 使用自然短句说明它是什么、为什么重要以及失败会造成什么
- 不输出“作用解释：”或带有多个字段标签的括号定义
- 英文采用官方大小写，数学符号、统计符号、单位和程序标识保持规范写法
- 公式、上下标、希腊字母、不等式和统计符号优先使用数学公式排版语法（LaTeX）
- 行内公式使用 `$...$`，独立公式使用 `$$...$$`
- 版本、状态、限制、期限和结果需要明确属于哪个对象
- 不写 `项目冻结版本`，需要写 `项目 Vivado 冻结版本` 或其他实际被冻结对象

## 5 数值来源

- 正文中的每个业务数值、测量结果、比较结果、阈值、配置值、预测值和经验值都要说明来源
- 引用外部资料时，在数值附近使用顺序引用编号，并在文末提供完整来源
- 计算结果需要写明输入数值、公式或换算方法，让读者能够重新计算
- 软件或实际应用产生的数值需要写明系统、界面、日志、配置文件、仪器或记录名称
- 用户提供的数值需要明确写成“根据用户本次提供的数据”
- 经验值和估计值需要明确标记，并说明经验样本、适用条件或不确定范围
- 同一来源覆盖一组连续数值时，可以在这组数值前统一说明，不需要逐项重复
- 章节编号、步骤编号、引用编号、版本标识和程序内部标识不属于业务数值，不要求额外来源
- 无法确认来源时，把数值标记为“来源待核对”，不能把它作为已经证实的事实

## 6 流程图

- 流程关系优先使用文本流程图（Mermaid）
- 默认使用 `flowchart TD`，让节点从上到下排列
- 横向比较更重要或竖向排列严重增加页面长度时，才改用横向排列
- 每个节点使用普通中文说明动作或状态，原文已有的内部名称与解释共同保留
- 流程图和图题默认共同居中，不使用空格模拟居中
- 图前说明为什么需要这张图，图后写编号图题并说明共同结论

## 7 图表

- 文档存在编号章节时，图表编号使用“所属一级章节号.本章序号”
- 第 1 章的前两个图依次写成“图 1.1”“图 1.2”，第 2 章的第一个表写成“表 2.1”
- 图形和表格在每个一级章节中分别从 `1` 开始计数，进入新章节后重新计数
- 文档没有编号章节时，图形和表格分别使用“图 1”“表 1”这种单一连续编号
- 图表编号不得从 `0` 开始，不使用“图 0.1”“表 0.1”或跨章节连续累计的编号
- 表格、图片、流程图及其题注默认在页面中共同居中
- 支持网页标签的 Markdown 轻量标记语言（Markdown）文档使用 `<div align="center">` 包住对象和题注，并在标签与对象之间保留空行
- Word 文字处理文档（Microsoft Word）、PDF 便携式文档格式（Portable Document Format）、幻灯片和网页使用目标格式的原生居中设置
- 纯轻量标记语言阅读器可能忽略网页标签；交付目标不支持居中时，需要明确说明版式限制并保留可读内容
- 不使用连续空格、全角空格或空白列模拟页面居中
- 表题统一放在表格上方，图题统一放在图片或流程图下方
- 编号章节中的表题写成“表 1.1 候选结果”，图题写成“图 1.1 验证流程”
- 补充说明放在图表下方，以“注：”开头
- 改编或转载内容在图题、表题或注释中给出引用编号
- 正式出版物另有模板时遵循出版模板
- 同一文档内的同类对象必须保持相同位置，除非指定模板明确要求例外

## 8 引用

- 引用采用 IEEE 电气电子工程师学会（Institute of Electrical and Electronics Engineers）顺序编码格式
- 正文按照首次引用顺序使用 `[1]`、`[2]`
- 重复引用同一来源时沿用原编号
- 文末参考文献按照编号顺序列出，并保证每个正文编号都有对应条目
- 不使用作者年份式引用

## 9 代码注释

- 所有面向用户展示的代码都需要注释
- 命令、配置项、键值项和彼此独立的语句属于列表式代码，每个有独立作用的代码行都使用该语言允许的同行注释
- 不得只注释重要行，也不得用一条总注释代替多条能够独立执行的语句
- 函数、类、条件、循环、查询和界面组件属于段落式代码，每个真实逻辑段开头使用该语言允许的独立注释
- 不能为了减少注释而把独立语句错误归类成连续逻辑段
- 注释说明当前代码的目的、关键输入或失败后果，不复述代码字面内容
- 注释不得破坏语法和可执行性
- 严格数据格式无法合法加入注释时，交付原文件链接，再提供带注释的等价示例

## 10 篇幅

- 简单问题优先使用一到三段
- 没有实际列举时不为了形式创建列表
- 同一逻辑框架中的短句不机械换行
- 同一独立章节只完整解释一次术语
- 同一解释单元只完整说明一次证据范围或发布边界
- 用户没有要求技术证据时，不主动增加原文没有出现的内部名称

## 11 标点

- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号
- 行内可以按语义使用逗号、分号和冒号
- 引出列表、引文、示例或后续正文时使用冒号
- 冒号、问号、感叹号和省略号可以保留在行尾
- 普通解释不使用装饰性引号

## 12 验证

- 实际问答测试至少覆盖极短、短、中、长和超长五种篇幅
- 同一行业、语气、受众或结构不得占据大多数样本
- 同时测试普通提问、愤怒质疑、混乱材料、紧急操作、专业审查和新手解释
- 同时测试纯正文、列表、条件分支、表格、公式、代码、时间线和完整报告
- 质量测试至少包含二十四组完整提问和完整回答
- 汇报最短字符数、最长字符数、平均字符数和字符数标准差
- 写作检查和样本差异检查都通过后，才能认定测试通过

## 13 交付

- 每个判断附近是否有原因或证据
- 每个行动是否说明目的和失败后果
- 每个动作、状态、判断和责任是否写明主体
- 多个对象同时出现后，代词是否仍然只有一个明确指向
- 前后句更换主体时，是否重新写明新主体
- 每个业务数值、测量结果、阈值、配置值、预测值和经验值是否写明来源
- 每个计算结果是否能够根据已写明的输入和公式重新计算
- 来源无法确认的数值是否明确标记为待核对
- 多章节内容是否使用从 `1` 开始的十进制层级编号
- 非问答内容的标题是否使用陈述式主题
- 操作步骤是否使用中文顺序，并在步骤之间保留空行
- 并列内容是否换行，同一逻辑框架中的短标签和值是否保持同行
- 版本、状态、限制、期限和结果是否写明所属对象
- 是否存在双重否定
- 图表编号是否与所属一级章节一致，并在新章节中从 `1` 重新计数
- 图表是否具有独立编号、符合文档用途的题注位置和必要注释
- 表格、图片、流程图及其题注是否使用目标格式支持的方式共同居中
- 引用是否使用顺序编码格式
- 每个术语是否在首次出现处自然解释
- 每个公式、上下标和数学符号是否使用数学公式排版语法
- 每个代码块是否采用列表式同行注释或段落式开头注释
- 流程关系是否优先使用从上到下排列的文本流程图
- 是否仍然存在字段标签式括号定义
- 是否仍然存在 `进行检查`、`进行分析`、`执行验证` 等名词化弱动词
- 一个长句是否承载了三次以上逻辑转折
- 是否为了让叙事完整而补写了用户没有提供的原因、结果或数据
- 同一解释单元是否反复申明相同证据边界或发布边界
- 同一主体的连续动作是否适合改成主体引导句和行动列表
- 是否存在行尾中文分号、中文句号、装饰性引号、写作套话、延迟主语、模糊指代、缺失动作主体或未翻译英文

复杂报告、术语争议或失败案例需要更多示例时，读取 `references/style-rules.md`
