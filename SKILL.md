---
name: human-readable-technical-writing
description: Create, rewrite, review, or validate user-facing Chinese technical prose for first-time readers. Use for reports, summaries, plans, status updates, notes, tables, and explanations containing technical terms, internal identifiers, causal conclusions, lists, or decision branches. Enforce cause-to-result reasoning, natural Chinese word order, multiline parallel content, indented branches, immediate term explanations, restrained punctuation, and first-reader validation.
---

# 中文技术内容人话写作

## 执行步骤

1. 先确定读者真正要判断或执行什么
2. 找出支撑判断的事实、原因和缺失证据
3. 按“原因或证据 → 后果 → 结论或行动”组织内容
4. 把术语改成普通中文；必须保留时，在首次出现处立即解释
5. 数学公式排版语法（LaTeX）使用标准结构显示公式和符号；把公式、上下标和数学符号改成这种格式
6. 把并列事实、列举对象和操作步骤拆成多行；再根据每个条件对应的结果数量安排分支
7. 为承担推理作用的列表补充引导句和承接段
8. 把代码分成列表式或段落式，再补充相应注释
9. 流程关系能够用图表达时，优先使用文本流程图（Mermaid），并默认从上到下排列
10. 按第一次阅读者的理解能力复查

## 控制结构

- 两个以上能够分别理解的事实、原因、对象、步骤或行动换行展示
- 名词列举也要换行；不可拆分的固定组合可以留在句内
- 一个条件只有一个结果时，把条件和结果写在同一项目中
- 一个条件对应多个行动或后果时，条件单独成项，后续内容换行缩进
- 多个条件不得写成连续的顶格段落
- 引出列表、引文、示例或后续正文时使用冒号
- 不用“先说结论、简单来说、需要注意的是”等套话引出内容
- 普通陈述采用“主体 → 动作或状态 → 结果”

## 控制术语

- 首次出现时使用“缩写 中文全称（英文全称）”或“中文全称（英文原名）”
- 术语解释放在第一次使用的位置；不得先使用数值或结论，最后才解释名称
- 英文采用官方大小写；数学符号、统计符号、单位和程序标识保持规范写法
- 公式、上下标、希腊字母、不等式和统计符号优先使用 LaTeX；行内使用 `$...$`，独立成行使用 `$$...$$`
- 正文只解释当前结论需要的内容；名称由来和背景细节按需放入注释或术语索引
- 内部状态先改写成中文结果；原始名称后移到证据部分
- 不输出“作用解释：”或带有多个字段标签的括号定义；把必要解释改成自然短句

## 控制流程图

- 流程关系优先使用文本流程图（Mermaid）
- 默认使用从上到下的排列方向，让阅读顺序和执行顺序一致
- 横向比较更重要或竖向排列严重增加页面长度时，才改用横向排列
- 每个节点使用普通中文说明动作或状态；内部名称只在确有核对需要时保留
- 图前说明为什么需要这张图，图后说明共同结论或下一步

## 控制篇幅

- 简单问题优先使用一到三段
- 没有实际列举时不要为了形式创建列表
- 用户没有要求技术证据时，不主动展开全部英文全称和内部名称
- 同一独立章节只完整解释一次术语

## 控制行尾

- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号
- 行内可以按语义使用逗号、分号和冒号
- 冒号、问号、感叹号和省略号可以保留在行尾

## 内容边界

- 直接回答完成状态时，立即说明依据和未证明范围
- 长报告先写普通中文结论，再写过程、失败和证据
- 表格使用中文结果，并为陌生术语提供一次人话解释
- 网址、界面原文和逐字证据保持原样，在外部说明输入、输出和失败后果

## 控制代码注释

- 命令、配置项、键值项和彼此独立的语句采用列表式注释；每个有独立作用的代码行使用该语言允许的同行注释
- 函数、类、条件、循环、查询和界面组件采用段落式注释；每个逻辑段开头使用该语言允许的独立注释
- 注释说明当前代码的目的、关键输入或失败后果，不复述代码字面内容
- 注释不得破坏语法和可执行性
- 严格数据格式或必须逐字复制的内容无法合法加入注释时，不展示无注释代码块；提供文件链接或正文说明，再给带注释的等价示例

## 验证样本差异

- 实际问答测试至少覆盖极短、短、中、长和超长五种篇幅
- 同一行业、语气、受众或结构不得占据大多数样本
- 同时测试普通提问、愤怒质疑、混乱材料、紧急操作、专业审查和面向新手的解释
- 同时测试纯正文、列表、条件分支、表格、公式、代码说明、时间线和完整报告
- 质量测试至少包含二十四组完整提问和完整回答，不得只用脱离语境的短句代替真实回答
- 汇报最短字符数、最长字符数、平均字符数和字符数标准差；字符数标准差表示不同回答的长度相差多大
- 只有写作检查与样本差异检查同时通过，才能认定测试通过

## 交付检查

- 每个判断附近是否有原因或证据
- 每个行动是否说明目的和失败后果
- 每个并列内容是否换行
- 单结果条件是否与结果保持在同一行
- 多结果条件是否换行并缩进
- 每个引出句是否使用冒号
- 每个术语是否在首次出现处解释
- 每个公式、上下标和数学符号是否使用 LaTeX
- 每个代码块是否采用列表式同行注释或段落式开头注释
- 流程关系是否优先使用从上到下排列的文本流程图（Mermaid）
- 是否仍然存在“作用解释：”或字段标签式括号定义
- 是否存在行尾中文分号、中文句号、装饰性引号、写作套话、延迟主语或未翻译英文

复杂报告、术语争议或失败案例需要更多示例时，读取 `references/style-rules.md`
