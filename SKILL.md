---
name: human-readable-technical-writing
description: Create, rewrite, review, or validate user-facing Chinese prose for first-time readers, especially technical content. Use for reports, summaries, plans, status updates, notes, tables, explanations, code explanations, and any Chinese output containing technical terms, internal identifiers, causal conclusions, figures, citations, numbers, or decision branches
---

# 面向智能体的人类可读技术写作

## 1 核心流程

第一步 确定读者需要判断或执行什么

第二步 找出支撑判断的事实、原因、后果和缺失证据

第三步 按照“具体原因或证据 → 实际后果 → 结论或行动”组织内容

第四步 根据第 3 节的任务条件，只读取当前任务需要的参考文件

第五步 保留原文中的正式术语和内部名称，并在首次出现后立即补充自然解释

第六步 写完草稿后检查完整正文，修复全部问题后重新检查

第七步 最终检查通过后直接交付，不再改动已经检查的文字

## 2 每次回答都要遵守的规则

### 2.1 因果关系

- 判断、风险、限制和建议先写具体原因或证据，再写实际后果，最后写结论或行动
- 直接回答完成状态、安全状态或批准结果时，可以先给结果，下一句立即提供依据和证据范围
- 每个判断附近都要有原因或证据，每个行动都要说明目的或失败后果
- 只重组已经提供的事实，不能补写没有来源的原因、结果、数据或责任判断

### 2.2 主体语序

- 普通陈述采用“主体 → 动作或状态 → 结果”
- 每个动作、状态、判断和责任都要写明主体
- 前文存在多个可能对象时，重复准确名称，不使用指向含糊的代词
- 前后句更换主体时，在发生切换的句子中重新写明主体
- 能用准确动词时，直接写“检查、分析、填写”，不用名词化弱动词拖长句子

### 2.3 直接表达

- 正文不得使用 `不是……而是……`、`并非……而是……` 和 `不在于……而在于……` 等否定先行转折
- 正文不得使用 `不能不`、`不得不`、`并非不` 和 `不是没有` 等双重否定
- 删除“先说结论、简单来说、需要注意的是”等写作套话
- 普通解释不使用装饰性引号
- 一个句子主要承载一个动作或一条因果关系，出现多次逻辑转折时拆句

### 2.4 技术信息

- 正式术语、英文缩写和内部名称必须保留，保证读者能够搜索原始证据
- 术语不能独自承担解释工作，首次出现时要用自然短句说明它是什么、为什么重要以及失败会造成什么
- 不输出“作用解释：”或由类型、含义、影响等字段标签拼成的括号定义
- 版本、状态、限制、期限和结果必须写明所属对象
- 业务数值、测量结果、阈值、配置值、预测值和经验值必须写明引用来源、计算来源、系统来源、用户输入来源或经验来源
- 来源无法确认的数值标记为“来源待核对”，不能作为已经证实的事实

### 2.5 内容形式

- 简单问题优先使用一到三段，没有实际列举时不创建列表
- 两个以上能够分别理解的事实、原因、对象或行动换行展示
- 同一逻辑框架中的简短标签和值保留在同一行
- 普通正文、列表项目和标题的行尾不使用中文分号或中文句号
- 引出列表、引文、示例或后续正文时使用冒号

## 3 按任务加载参考文件

- 出现多个章节、操作步骤、条件分支、表格、图片、流程图或引用时，读取 `references/structured-documents.md`
- 出现英文缩写、内部名称、数学公式、数值证据或代码时，读取 `references/technical-content.md`
- 编写完整报告、审计材料、复盘、交接文档或其他长篇交付物时，读取 `references/complex-reports.md`
- 用户指出写作问题、规则发生冲突或需要查看正反示例时，读取 `references/style-rules.md`
- 修改本技能、检查器、案例或仓库质量流程时，读取 `references/quality-development.md`
- 普通回答不得加载质量测试说明，也不得运行技能开发测试
- 只读取当前任务命中的参考文件，不能为了保险一次加载全部参考文件

## 4 交付门禁

- 否定先行转折属于硬失败，检查范围包括同一行、跨行和跨段内容
- 任何位置命中后，删除原句框架，按照具体原因、实际后果和结论重新组织
- 修改后从正文开头重新扫描，不能只修复检查器发现的第一处
- 具有两个以上编号章节、包含图表或代码、或者正文超过八百个汉字的内容属于大型任务
- 大型任务必须把准备交付的完整正文交给 `scripts/Test-HumanReadableChinese.ps1` 检查
- 检查脚本返回 `PASS` 后才能交付；检查后发生任何文字改动时，修改后的完整版本必须重新检查
- 对话环境无法运行脚本时，先逐节检查，再对拼接后的全文检查一次
