# 补丁冲突处理

## 1. 冲突定义

两个补丁修改的字符区间发生重叠时构成冲突；同一旧文本由多个补丁重复修改也构成冲突

## 2. 批量规则

- 互不重叠并绑定同一文档摘要的补丁可以进入同一批事务
- 修改同一段但区间不重叠的补丁可以同批提交，但全部范围仍根据原始文档计算
- 区间重叠时整批拒绝，不猜测执行顺序
- 任一补丁依赖的旧文本已经变化时，补丁失效并重新生成

## 3. 结果

冲突处理只返回通过的编辑区间或结构化失败；它不进行部分提交，因为部分写入会让文档与原始摘要失去对应关系
