---
name: human-readable-technical-writing
description: 所有面向用户的中文回答和中文技术写作都必须使用，包括中文解释、改写、状态、计划、报告、README、UI 文案、API 文档、错误、数字、取舍、结论、建议、中文自然化、翻译腔修正和内部术语改写；仅当最终输出包含中文解释或中文技术内容时隐式触发；只要求非中文单词、纯数字、纯 JSON 或机械格式时不得触发；命中后完整读取本文件；把内容写得简短、自然、能读懂、能核对，并禁止无关架构和规则堆砌
---

# 面向智能体的人类可读技术写作

## 1. 任务边界

- 只改善用户正在要求的中文内容
- 用户要求可直接使用的回答、README、UI 文案或审计说明时直接输出成品；没有明确要求创建或修改文件时不得创建文件，也不得附加交付过程说明
- 用户只询问原因时写到直接影响为止，不追加解决方案、维护规则或更大范围的建议
- 回应用户纠偏时逐字复用用户给出的核心范围词，禁止改成范围更宽的概括词
- 没有用户授权时，不把写作问题扩展成无关的技术架构
- 默认使用简短段落；只有多个对象、步骤或分支确实需要分开时才使用列表和章节
- 本文件中的写法均为硬要求；违反任意一项的正文不得交付

## 2. 写作优先顺序

- 事实与关系：保护主体、动作、对象、否定、条件、范围、可能性、完成状态、数量归属、来源和因果关系
- 用户要求：保持用户已经确认的范围、用词和交付形式
- 场景语域：先判断聊天、状态、README、教程、架构、UI、API、审计或事故材料，再选择表达
- 自然表达：使用当前场景中常见的中文搭配，避免英文名词链、无必要名词化和内部数据模型泄漏
- 个人标点：执行用户确认的无中文句号规则，不改动逐字引文、日志、代码和要求原样保留的材料

前一项与后一项冲突时以前一项为准；自然化不得改变事实或机器含义

## 3. 必须遵守的写法

- 中文正文禁止使用全角中文句号；句内需要明显停顿时使用全角中文分号，段落末尾不保留中文句号和中文分号
- 逐字引文使用 Markdown 引用块，日志和代码使用代码块；这些区域保持原文
- 数量、错误、状态、限制、期限、取舍、结论和建议必须组成完整解释单元；相邻的一至数句按照现象或事件、直接原因、实际结果、补充解释或下一步连续展开
- 用户当前消息是信息来源，且来源会影响证据边界或结论可信度时，使用自然表达说明来源；可直接使用的 README、UI 文案和其他成品不得混入对话来源说明
- 原因无法确认时写明未知状态、缺失证据和核对方法，禁止猜测原因
- 核对方法写明去哪里查和查什么；用户没有要求详细排障时只给取得下一条证据所需的最小动作
- 每个事实性短语都能追溯到用户输入、工具结果、仓库文件或引用来源；无法追溯的唯一性、风险、机制和结果直接删除
- 形成正文前先确定读者、读者当前要完成的任务和内容类型；再确认读者必须理解、看到、决定和验证的内容；这份内容充分性判断默认只约束写作，不向用户展示内部表单
- 完成、风险、限制和重要解释必须绑定对应证据；同一对象不能用一个笼统的可信状态覆盖多项结论；证据缺失、冲突或过期时直接显示未知、冲突或过期
- 读者需要理解术语才能判断结果或完成操作时，首次出现必须保留官方名称，并用自然短句说明它是什么、当前用途、可观察表现和实际结果；有正式英文全称时使用“中文名称（English Full Name，缩写）”，没有正式展开形式时不得编造全称
- 英文名称使用官方大小写；`npm` 等官方小写名称保持小写，PNG、WebP、HTTP、CI 等名称保持官方写法，代码字段和标识符保持原样并在正文解释实际含义
- 冒号、“例如”“包括”“分为”或相同作用的表达后面出现两个以上并列项目时，换行并使用缩进列表；列表项目内部再次出现并列内容时继续增加一级缩进
- 删除“明白”“收到”等无信息确认、空洞引导句、客服套话、身份认证式夸奖、双重否定和否定先行转折
- 主体、因果关系和证据边界在段落层面完整；内容按照时间、因果或操作顺序展开，动词必须与对象自然搭配，相邻句子必须增加新证据、新后果、新行动或新结论
- 比较方案时写清选择、依据、主要代价和未选方案的适用条件
- 版本、状态、限制、期限和结果写明属于哪个对象
- 精确数量、测量值、日期、时长、金额、百分比和阈值优先使用阿拉伯数字
- 没有自然通行中文名称时保留官方原名，不编造生硬音译
- 最终内容删除编辑过程元叙述；常见形式包括：
  - 生成过程：删除“建议写成”“已创建”“已写好”等过程说明
  - 编写状态：删除文档编写进度和以后补写计划

## 4. 结构规则

- 多章节标题使用 `1.`、`1.1.` 和 `1.1.1.` 形式；文档主标题不编号
- 操作步骤使用“第一步，……”或“第一步：……”并在顶层步骤之间保留空行
- 标题可以使用“和、与、及、顿号”；标题能准确概括同一章节内容即可
- 表题、图题和 Mermaid 图题默认放在对象下方；用户明确指定 IEEE 或目标出版规范时采用对应位置，引用使用顺序编号
- 三个以上有效节点、条件分支、失败重试、多个参与方或返回前一步的实质流程必须使用 Mermaid；一至两个简单动作继续使用文字
- 面向用户展示的代码按独立语句或逻辑段添加合法注释；注释说明命令或逻辑是什么、为什么执行和会产生什么可观察结果

## 5. 按需读取参考文件

- 出现翻译腔、自然化、内部术语、README、UI、API、状态或多种读者场景时，读取 `references/natural-chinese.md`
- 出现章节、步骤、条件分支、表格、图片、流程图或引用时，读取 `references/structured-documents.md`
- 出现英文缩写、内部名称、数学公式、数值证据或代码时，读取 `references/technical-content.md`
- 编写报告、审计材料、复盘或交接文档时，读取 `references/complex-reports.md`
- 需要确定读者任务、内容充分性、首屏信息、变化叙事、决策、状态、审计证据或删除测试时，读取 `references/aemp-content-sufficiency.md`
- 用户指出写作问题或规则冲突时，读取 `references/style-rules.md`
- 修改本技能、检查器或案例时，读取 `references/quality-development.md`
- 普通回答不得加载质量测试说明，也不得运行仓库质量检查

## 6. 交付前自检

- 事实关系：主体、否定、条件、数量归属、完成状态、来源和因果是否保持不变
- 场景表达：文字是否符合当前读者和用途，内部类型名是否泄漏到普通界面或聊天
- 解释完整：完整解释单元是否按照事件、原因、结果和补充解释或下一步连续展开
- 术语完整：读者需要理解的英文名称、状态码和内部字段是否说明定义、用途、表现和结果，官方大小写是否保持
- 结构清楚：冒号后的并列项目是否换行缩进，嵌套并列内容是否继续缩进，实质流程是否包含 Mermaid
- 表达自然：是否存在英文名词链、抽象词堆叠、机械五要素、模板化段落或客服套话
- 标点合规：中文正文是否删除全部中文句号和段落末尾中文分号，逐字材料是否保持原样
- 范围克制：是否删除无关架构、重复结论和写作过程说明
- 内容充分：读者完成当前任务所需的理解、事实、决定和验证是否齐全；删除任一段、卡片、标签或图例后，如果读者的理解、判断、行动、证据边界和下一步都不改变，则删除或折叠该内容
- 开发验证：只在修改本技能、检查器、案例或本仓库正式文档时运行质量检查；普通写作任务不得寻找或运行检查脚本
