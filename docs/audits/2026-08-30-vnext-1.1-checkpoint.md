# vNext 1.1 候选阶段检查点

## 1. 当前决定

当前决定为继续到候选锚点审核节点；原因是用户提供的 vNext 1.1 计划已经明确任务模型、来源分层、精确补丁和人工金标边界；实际影响是本阶段可以建设可检查基础，但不能在人工审核以前宣布风格已经验证

## 2. 内部证据

- 权威设计：`docs/design/vnext-1.1-authoritative-plan.md`
- 计划文件摘要：`451335f8e7ffce081b434b8e307e02bb802da2b209b0b38953e42d146a069751`
- 干净基线：`origin/main` 的 `4451feaf8c9a333dfe590d044dc5daa13e60c50d`
- 旧工作区：保留 `22` 个已跟踪改动和 `5` 个未跟踪文件；原因是它们属于上一代方案，直接迁入会重新引入已被替换的设计；实际影响是旧工作保持可追溯，但不进入 vNext 候选分支

## 3. 外部约束

- Codex Skill 的核心入口应保持精简，并通过明确链接按需加载资源；该约束来自当前 Skill 创建规范；实际影响是 vNext 不能把全部运行说明重新堆回 `SKILL.md`
- 生成式写作不能由同一模型自写自证；该限制与用户的人工金标要求一致；实际影响是候选样本只能等待审核

## 4. 可证伪假设

- 假设一：基础操作与解释增量可以覆盖首批 12 个案例；若案例需要第三个互相耦合的任务轴，任务契约必须重新设计
- 假设二：来源单元和支持映射能够暴露信息遗漏；若用户仍无法判断补充内容来自哪里，呈现协议必须调整
- 假设三：精确补丁能够避免非目标区域变化；若事务测试出现任何越界修改，提交器不得进入下一阶段

## 5. 当前停止条件

12 个候选锚点、契约结构和最小提交器验证完成后停止；下一步由用户逐个接受、拒绝或要求修改，随后才能形成第一批金标并实现完整运行时
